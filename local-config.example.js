// Copie este arquivo para `local-config.js` e insira seu e-mail de administrador.
// Este arquivo é ignorado pelo Git (veja .gitignore).
// Exemplo:
// window.localConfig = { OWNER_EMAIL: "seu-email@exemplo.com" };

window.localConfig = {
  OWNER_EMAIL: "seu-email@exemplo.com"
};
